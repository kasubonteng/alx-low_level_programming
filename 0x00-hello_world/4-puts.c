#include <stdio.h>

/**
 * main - writes every character in the string followed by new line to stdout
 *
 * Return: 0 if successful
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");

	return (0);
}
